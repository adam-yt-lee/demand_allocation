# 齊套料管理工具 - 規格書 v2

## 目標

單一 HTML 離線工具，用於管理缺料狀態下的需求齊套計算。
使用者上傳 XLSX 檔案（含三個分頁），手動拖拉調整需求優先序、勾選是否列入計算，系統即時計算哪些需求可滿足、哪些缺料、剩餘庫存。

---

## 資料輸入：XLSX 上傳

### 檔案格式

單一 XLSX 檔案，包含三個 Sheet（靠 sheet name 辨識）：

#### Sheet: `Demand`（需求表）

| 欄位 | 型別 | 說明 |
|------|------|------|
| Demand | string | 需求名稱，**unique key** |
| SPEC | string | 料號 |
| Usage | number | 每套需求消耗該 SPEC 的數量 |
| Qty | number | 需求套數 |
| Month | number | 需求月份（格式：YYMM，例如 2605 = 2026年5月） |

同一 Demand 會有多列（每列一個 SPEC），所有 SPEC 都必須滿足才算齊套。

#### Sheet: `Stock`（庫存表）

| 欄位 | 型別 | 說明 |
|------|------|------|
| SPEC | string | 料號 |
| Qty | number | 現有庫存數量 |

庫存永遠可用，不受月份限制。

#### Sheet: `Incoming`（進料表）

| 欄位 | 型別 | 說明 |
|------|------|------|
| SPEC | string | 料號 |
| Qty | number | 進料數量 |
| Month | number | 到料月份（格式：YYMM） |

進料可用條件：**進料 Month <= 需求 Month**（料要先到才能用）。

### 防呆機制

1. **Sheet name 檢查**：上傳後檢查是否包含 `Demand`、`Stock`、`Incoming` 三個分頁（大小寫不敏感）
2. **Header 欄位檢查**：每個 sheet 檢查必要欄位是否存在（大小寫不敏感、trim 空白）
3. **資料型別驗證**：Usage / Qty / Month 必須為正數，非數字者標紅提示
4. **空值檢查**：必要欄位不得為空
5. **Demand 一致性**：同一 Demand 的 Qty 和 Month 必須相同（多列 SPEC 屬同一需求）
6. 錯誤訊息明確指出哪個 sheet、哪一列、哪個欄位有問題

### 技術

使用 SheetJS (xlsx.js) 從 CDN 載入，讀取 XLSX。

---

## UI 佈局

```
┌────────────────────────────────────────────────────────────┐
│  齊套料管理工具                          [上傳 XLSX 按鈕]    │
├──────────────────────────┬─────────────────────────────────┤
│                          │                                 │
│  需求卡片列表（左側）       │  總結面板（右側）                 │
│  可拖拉排序 + 勾選         │                                 │
│                          │  ┌─────────────────────────┐   │
│  ┌──────────────────┐    │  │ 總覽                     │   │
│  │ ☑ #1 Demand A    │    │  │ 可滿足：3 / 總需求：5     │   │
│  │   2605 | 100 套   │    │  │ 缺料：1 | 凍結：1        │   │
│  │   Item1, Item2    │    │  └─────────────────────────┘   │
│  │   ✅ 可滿足       │    │                                 │
│  └──────────────────┘    │  ┌─────────────────────────┐   │
│  ┌──────────────────┐    │  │ 剩餘庫存總表              │   │
│  │ ☑ #2 Demand B    │    │  │                           │   │
│  │   2606 | 50 套    │    │  │ SPEC | 庫存 | 進料 | 剩餘 │   │
│  │   Item1, Item3    │    │  │ Item1| 150 | 200 | xxx  │   │
│  │   ❌ Item3 缺150  │    │  │ Item2|1000 |   0 | xxx  │   │
│  └──────────────────┘    │  │ ...                       │   │
│  ┌──────────────────┐    │  └─────────────────────────┘   │
│  │ ☐ #3 Demand C    │    │                                 │
│  │   2607 | 30 套    │    │                                 │
│  │   Item4           │    │                                 │
│  │   （凍結，不計算）  │    │                                 │
│  └──────────────────┘    │                                 │
│                          │                                 │
├──────────────────────────┴─────────────────────────────────┤
│  錯誤/警告訊息區                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 卡片設計

每張需求卡片顯示：

| 元素 | 說明 |
|------|------|
| 勾選框 | 左側 checkbox，勾選=列入扣料計算，取消勾選=凍結（不扣料） |
| 優先序 | #1, #2, #3...（依拖拉順序） |
| Demand 名稱 | 粗體 |
| Month | 需求月份 |
| Qty | 需求套數 |
| SPEC 列表 | 該需求包含的所有料號 |
| 狀態 | ✅ 可滿足 / ❌ 缺料明細 |

### 卡片視覺狀態

| 狀態 | 視覺 |
|------|------|
| ✅ 可滿足（已勾選） | 正常色彩，綠色標記 |
| ❌ 缺料（已勾選） | 正常色彩，紅色標記 + 缺料明細 |
| ☐ 凍結（未勾選） | 灰階，半透明，不顯示狀態 |

所有卡片都可拖拉調整順序，包括灰階/缺料的。

---

## 核心邏輯：齊套計算

### 觸發時機

即時自動重算：
- 拖拉調整順序後
- 勾選/取消勾選後
- 上傳新檔案後

### 扣料流程

```
初始化：
    庫存池 = 複製庫存表（每個 SPEC 的 Qty）
    進料池 = 複製進料表（每筆獨立，含 SPEC / Qty / Month / 剩餘Qty）

FOR 每筆 Demand（按拖拉優先序）:
    IF 未勾選 → 跳過，標記「凍結」
    
    計算該 Demand 每個 SPEC 的可用量：
        可用量 = 庫存池[SPEC] + SUM(進料池中 SPEC 相符 且 Month <= Demand.Month 的剩餘Qty)
    
    需求量 = Usage × Qty（每個 SPEC 各自計算）
    
    檢查齊套：
        shortages = []
        FOR 每個 SPEC:
            IF 可用量 < 需求量 → shortages.push({ SPEC, 缺少: 需求量 - 可用量 })
    
    IF shortages 為空：
        標記「✅ 可滿足」
        執行扣料：
            FOR 每個 SPEC:
                剩餘需扣 = Usage × Qty
                1. 先扣庫存池[SPEC]，扣到 0 為止
                2. 不夠 → 按月份升冪逐筆扣進料池（僅限 Month <= Demand.Month）
    ELSE：
        標記「❌ 缺料」+ shortages 明細
        **不扣任何料**
```

---

## 右側總結面板

### 總覽區

| 項目 | 說明 |
|------|------|
| 可滿足數量 | 已勾選且齊套的需求數 |
| 缺料數量 | 已勾選但缺料的需求數 |
| 凍結數量 | 未勾選的需求數 |
| 總需求數 | 所有需求數 |

### 剩餘庫存總表

| 欄位 | 說明 |
|------|------|
| SPEC | 料號 |
| 原始庫存 | 庫存表的 Qty |
| 已扣庫存 | 被滿足需求扣掉的庫存量 |
| 剩餘庫存 | 原始庫存 - 已扣庫存 |
| 原始進料 | 進料表合計（所有月份） |
| 已扣進料 | 被滿足需求扣掉的進料量 |
| 剩餘進料 | 原始進料 - 已扣進料 |
| 可用合計 | 剩餘庫存 + 剩餘進料 |

---

## 技術規格

- 純 HTML + CSS + JavaScript，單一檔案
- SheetJS (xlsx.js)：從 CDN 載入，用於讀取 XLSX
- 拖拉排序：HTML5 Drag and Drop API（不引入額外 library）
- 不需後端、不需網路（SheetJS 載入後可離線運作）

---

## 測試資料

### Sheet: Demand
```
Demand	SPEC	Usage	Qty	Month
A	Item1	2	100	2605
A	Item2	16	100	2605
B	Item1	3	50	2606
B	Item3	5	50	2606
```

### Sheet: Stock
```
SPEC	Qty
Item1	150
Item2	1000
Item3	100
```

### Sheet: Incoming
```
SPEC	Qty	Month
Item1	200	2605
Item2	500	2606
Item3	300	2607
```

### 預期結果（A 優先於 B，皆勾選）

**Demand A (Month 2605)：**
- Item1 需 2×100=200，可用=庫存150 + 進料200(2605<=2605)=350 → 夠
- Item2 需 16×100=1600，可用=庫存1000 + 進料0(無<=2605的進料)=1000 → **缺600**
- → ❌ 缺料，不扣料

**Demand B (Month 2606)：**
- Item1 需 3×50=150，可用=庫存150 + 進料200(2605<=2606)=350 → 夠
- Item3 需 5×50=250，可用=庫存100 + 進料0(2607>2606)=100 → **缺150**
- → ❌ 缺料，不扣料

**剩餘：** 全部未扣（兩筆都缺料）
